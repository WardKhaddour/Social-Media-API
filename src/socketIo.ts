const ioEvents = {
  POST: 'post',
  LIKE: 'like',
  COMMENT: 'comment',
  FOLLOW: 'follow',
};

const ioActions = {
  CREATE: 'create',
  UPDATE: 'update',
  DELETE: 'delete',
};

export { ioEvents, ioActions };
